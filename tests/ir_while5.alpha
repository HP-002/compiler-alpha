type void : integer -> integer
function entry : void

entry(arg) := {
    [ integer : x ; integer : y ]
    
    x := 0;

    while (x < 10) {
        y := 0;

        while (y < 2) {
            x := x + 1;
        }
    }

    while (x < 30) {
        x := x + 2;
    }

    return x;
}
