type void : integer -> integer
function entry : void

entry(arg) := {
    [ integer : x ]
    
    x := 0;

    while (x < 2) {
        x := x + 1;
    }

    while (x < 10) {
        x := x + 2;
    }

    return x;
}
