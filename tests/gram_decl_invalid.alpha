(* invalid_blocks.alpha *)

func1(x) := {
    integer: a;
    return a;
}

func2(x) := {
    [ integer: a  boolean: b ]
    return x;
}

func3(x) := {
    [ integer a ]
    return x;
}

func4(x) := {
    x := 5;
    [ integer: a ]
    return x;
}

func5(x) := {
    [ integer: a; boolean: b; ]
    return x;
}

entry(arg) := {
    return 0;
}